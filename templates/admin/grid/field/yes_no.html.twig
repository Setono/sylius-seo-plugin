{% if data %}
    <span class="badge bg-success text-uppercase">{{ 'sylius.ui.yes'|trans }}</span>
{% else %}
    <span class="badge bg-danger text-uppercase">{{ 'sylius.ui.no'|trans }}</span>
{% endif %}
