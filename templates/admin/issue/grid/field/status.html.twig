{% set colors = {'open': 'bg-azure', 'ignored': 'bg-secondary', 'resolved': 'bg-success'} %}
<span class="badge {{ colors[data.value]|default('bg-secondary') }} text-uppercase">{{ ('setono_sylius_seo.status.' ~ data.value)|trans }}</span>
